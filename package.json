{
  "name": "a2-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "dev": "pnpm -rF a2z dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "tsup": "^7.2.0"
  }
}